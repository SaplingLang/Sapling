package dev.npex42.sapling.tokens;

public enum TokenType {
    INTEGER,
    IDENTIFIER,
    STRING,

    LET,
    PRINT,
    FN,
    PUBLIC,
    PRIVATE,

    PLUS,
    MINUS,
    SLASH,
    STAR,
    SEMICOLON,

    LPAREN,
    RPAREN,
    LCURLY,
    RCURLY,
    COMMA,

    EQUAL,
    EOF
}
